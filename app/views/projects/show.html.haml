= title("Task lists of #{@project.name}")

%ul.list-group-item
  - @task_lists.each do |task_list|
    %li.list-group-item
      %h5.list-group-item-heading
        = task_list.name

%span.pull-right
  = link_to '+', new_project_task_list_path(@project.id), class: 'btn btn-primary'
