= title('My Projects')

- @projects.each do |project|
  %div.panel.panel-default
    %div.panel-heading
      = link_to(glyphicon('tasks'), project_path(project.id), class: 'pull-right')
      %h4.panel-title.clearfix
        = project.name
    %div.panel-body
      = description(project.description)
    %div.panel-footer
      %small.pull-right
        = created_on(project.created_at)
      = link_to 'Add task list', new_project_task_list_path(project.id), class: 'btn btn-primary'
